#ifndef UTIL_H
#define UTIL_H

#include <vector>
#include <string>

std::vector<std::string> split(const std::string& s);
bool is_indicator(const std::string& s);
int nrand(int n);

#endif // !UTIL_H

