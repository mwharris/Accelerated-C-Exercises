#include "stdafx.h"
#include <iostream>

#include "Equal.h"
#include "Find.h"
#include "FindIf.h"
#include "Search.h"
#include "Copy.h"
#include "RemoveCopy.h"
#include "RemoveCopyIf.h"
#include "Remove.h"
#include "Transform.h"
#include "Partition.h"
#include "Accumulate.h"

using std::cout;
using std::cin;
using std::endl;

int main()
{
	/*
	test_equal(cout);
	test_find(cout);
	test_copy(cout);
	test_remove_copy(cout);
	test_search(cout);
	test_find_if(cout);
	test_remove_copy_if(cout);
	test_remove(cout);
	test_transform(cout);
	test_partition(cout);
	*/
	test_accumulate(cout);
    return 0;
}

